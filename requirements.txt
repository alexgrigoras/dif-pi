# Synthetic Data Generation (SDG) module for DIF-PI
# Core numerical / notebook stack
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
jupyter>=1.0
notebook>=7.0
nbformat>=5.9
ipywidgets>=8.0

# PyTorch + Hugging Face stack
# For Apple Silicon / CPU training
torch>=2.4
transformers>=4.52
accelerate>=0.34
datasets>=3.0
sentencepiece>=0.2.0
protobuf>=4.25
safetensors>=0.4
huggingface-hub>=0.24

# Optional PEFT / QLoRA extras
peft>=0.12
# bitsandbytes is primarily for CUDA/NVIDIA environments
bitsandbytes>=0.45; platform_system != "Darwin"
